<% content_for :title do %>
	LineHive: Create a timeline
<% end %>

<% content_for :stylesheets do %>
	<%= stylesheet_link_tag "new" %>
	<%= stylesheet_link_tag "form" %>	
	<%= stylesheet_link_tag "overview" %>	
	<%= stylesheet_link_tag "timeline" %>	
	<%= stylesheet_link_tag "smoothness/jquery.ui.datepicker.css" %>	


<% end %>


<% content_for :javascripts do %>

<%= javascript_include_tag "timeline.js" %>
<%= javascript_include_tag "jquery.ui.datepicker.js" %>
<%= javascript_include_tag "jquery.easing.1.3.js" %>
<%= javascript_include_tag "jquery.create.js" %>
<%= javascript_include_tag "jquery.autoellipsis.js" %>
<%= javascript_include_tag "jquery.mousewheel.js" %>



<script type="text/javascript" charset="utf-8">

	function add_url() {
		console.log("here");
		
		
		var text = $("#urls").val();
		console.log(text);
		$("#loader").show();
		
		
		jQuery.ajax({
        type: "POST",
        url: "<%= url_for :controller => "trails", :action => "process_urls"  %>",
        dataType:"json",
        data:{urls : text },
        success:function(data){
					
					$("#loader").hide();
					
					console.log(data);
					
					for(i in data) {
						timeline.add(data[i]);
					}
					
					timeline.draw();
        },
        error:function (xhr, ajaxOptions, thrownError){
					alert("Error Occured");
        }    
    });
    
		$("#urls").text("");
		
	}
	
	function add_url_keydown(event) {
		if(event.which == 13) {
			add_url();
			return false;
		}
	}
	
	function trail_close() {
		$(this).parents(".article").remove();
	}
	
	
	
	function next_thumbnail() {
		console.log("next");
				
		var li = $(this).parents(".article").find("li.selected");
		$(li).removeClass("selected");
		$(li).next().addClass("selected")
		
		if($(li).siblings(".selected").size() == 0) {
			$(this).parents(".article").find("li:first").addClass("selected");
		}
		
		 var url = $(this).parents(".article").find("li.selected").text();
		$(this).parents(".article").find(".thumbnail img").attr("src", url);
		
		
		
		return false;
		
	}
	
	function prev_thumbnail() {
		console.log("prev");
		
		
		var li = $(this).parents(".article").find("li.selected");
		$(li).removeClass("selected");
		$(li).prev().addClass("selected")
		
		if($(li).siblings(".selected").size() == 0) {
			$(this).parents(".article").find("li:last").addClass("selected");
		}
		
		 var url = $(this).parents(".article").find("li.selected").text();
		$(this).parents(".article").find(".thumbnail img").attr("src", url);
		
		return false;
	}

	$(function() {
		
		timeline.use_clustering = false;
		
		<% if @articles %>
			timeline.init(<%= @articles.to_json %>);
		<% else %>
			timeline.init();		
		<% end %>
		

		
		$('.article .next').livequery('click', next_thumbnail);
		$('.article .prev').livequery('click', prev_thumbnail);
		$('.article .close').livequery('click', trail_close);
		
		
		$("#urls").keypress(add_url_keydown);
		$("#add_urls").click(add_url);
		
		$(".url_text").Watermark("url");
		$(".subtitle_text").Watermark("your thoughts...");   
		
		
		$("#post_back").submit(function() {
			
			//console.log("here");
			
			
			var data = { title: "", links: [] };
			data["title"] = $("#title").val();
			
			$("#timeline .event").each(function(item) {

				var headline = $.trim($(".info .headline", this).html());
				var url = $.trim($(".info .url", this).html());
				var source = $.trim($(".info .source", this).html());
				var image = $.trim($(".info .image", this).html());
				var date = $.trim($(".info .date", this).html());
								
				// TODO: I need to standardize this json string. It matches the active record model...
				var item = {
					headline: headline,
		   		url: url,
		   		source: source,
		   		image_url: image,
		   		date: date
		   	};
		
				//console.log(item);
				
		
				data["links"].push(item);				
				
				
				
			});
			
			var string = JSON.stringify(data);
			
			$("#trail_input").val(string); 
			
						
		});		
		
	});
</script>




<% end %>


<h2>Create New Timeline <span style="font-size:12px; font-weight:normal">(<%= link_to "Other ways to create timelines", :controller => "home", :action => "jetpack" %>)</span></h2>

<div class="space_10"></div>

<fieldset>

	<div class="small_columns">
		<div class="maincol">
			<label for="title" class="large_label" >Timeline Title:</label>
			<input type="text" name="title" value="<%= params[:title] %>" style="width:100%" id="title" class="large_text">
		</div>
		<div class="sidecol">
			<div class="round_box" style="margin-top:15px">
				150 Characters
			</div>
		</div>
		<div class="clear"></div>
	</div>

</fieldset>


<fieldset style="">
	
	<div class="small_columns">
		<div class="maincol">
			
			<%= image_tag "loader.gif", :style => "float:right;", :id => "loader" %>
			<label for="urls" class="large_label" >URLs:</label>
						
			<div id="articles"></div>
			
			<input type="button" id="add_urls" class="url_add_button" value="Add">
			<textarea name="urls" id="urls" id="urls" class="large_text" style="width:520px;resize:none;height:75px;"></textarea>
			
			<div class="space_20"></div>
			
		</div>
		<div class="sidecol">
			<div class="round_box" style="margin-top:15px">
				Type or paste in as many URLs as you want and hit <b>Enter</b> to add them to your trail.
			</div>
		</div>
		<div class="clear"></div>
	</div>
	
	
</fieldset>


<%= render :partial => "timeline", :locals => { :noedit => false } %>

<div id="buttons">
				
		<% form_tag({:controller => "trails", :action => "create" }, {:id => "post_back"})  do %>
			<%= submit_tag "Create", { :class => "trail_button"} %>
			<input type="hidden" name="trail" value="" id="trail_input"> 
		<% end %>
	
</div>


